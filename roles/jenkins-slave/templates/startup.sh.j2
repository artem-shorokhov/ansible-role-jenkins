#!/bin/bash

nohup java -jar {{ jenkins_slave_home }}/agent.jar -jnlpUrl http://{{ jenkins_master_ip }}:{{ jenkins_port }}/computer/{{ jenkins_slave_name }}/slave-agent.jnlp {{ webpage.content | regex_search('-secret\\s[a-zA-Z0-9]+') }} -workDir {{ jenkins_slave_home }} &
